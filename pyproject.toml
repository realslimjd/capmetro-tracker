[project]
name = "capmetro-tracker"
version = "0.1.0"
description = "Create a PNG of the Austin public network"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "geopandas>=1.1.1",
    "matplotlib>=3.10.8",
    "pillow>=12.0.0",
    "requests>=2.32.5",
]
